# OpenAI Agents SDK Python：项目地图与源码阅读路线

本文面向有 Python 和计算机基础、第一次系统阅读大型 Python SDK 的开发者。目标是先建立整体地图，找到核心主干，理解一次 Agent 如何运行，再决定哪些源码值得深入。

分析日期：2026-09-05。分析依据：本地检出的提交 `3e0e8937`，本地 `pyproject.toml` 标记版本为 `0.22.0`。本文依据仓库目录与文件清单，以及公共入口、运行循环、模型适配、工具、交接、Session 和相关示例的重点阅读整理；这是学习导览，不是对所有源码的逐行审计。后续源码变化可能使具体位置和实现细节发生变化。

> **Agent 描述“谁来做、能做什么”；Runner 负责“让它持续做下去”；Model 提出回答或行动请求；SDK 执行工具、处理交接，直到得到结果。**

## 目录

1. [这个仓库到底是什么](#第一部分这个仓库到底是什么)
2. [整个仓库的地图](#第二部分整个仓库的地图)
3. [按角色拆解 src/agents](#第三部分按角色拆解-srcagents)
4. [SDK 的主干，以及谁控制循环](#第四部分sdk-的主干以及谁控制循环)
5. [沿着 hello_world 追一次源码](#第五部分沿着-hello_world-追一次源码)
6. [为什么拆成 run.py 和 run_internal](#第六部分为什么拆成-runpy-和-run_internal)
7. [普通 Python 函数如何成为 Tool](#第七部分普通-python-函数如何成为-tool)
8. [Handoff 与 Agent as Tool](#第八部分handoff-与-agent-as-tool)
9. [Session / Memory 到底是什么](#第九部分session--memory-到底是什么)
10. [哪些现在可以不学](#第十部分哪些现在可以不学)
11. [按本地 examples 安排学习路线](#第十一部分按本地-examples-安排学习路线)
12. [学习版项目地图](#第十二部分学习版项目地图)
13. [先抓哪 5～8 个文件或目录](#最后先抓哪-58-个文件或目录)

## 第一部分：这个仓库到底是什么

这是一个**帮助开发者构建 Agent 应用的 Python 库**。

如果直接调用模型 API，你通常要自己处理：

- 把用户消息发送给模型。
- 检查模型是否要求调用工具。
- 执行工具，把结果再发给模型。
- 判断任务是否完成。
- 保存历史、切换 Agent、记录执行过程。

这个 SDK 把这些共同工作整理成可复用的组件。你主要编写业务指令和业务工具，SDK 负责运行机制。

它本身不包含大语言模型的权重，也不是一个现成聊天网站。

### 安装后，实际用了仓库哪部分？

本地 [pyproject.toml](pyproject.toml) 明确配置：

```toml
packages = ["src/agents"]
```

因此，可以这样理解：

```text
仓库中的 src/agents/
        ↓ 打包
名为 openai-agents 的 Python 安装包
        ↓ 安装
Python 环境中的 agents 包
        ↓
from agents import Agent, Runner
```

**安装名是 `openai-agents`，导入名是 `agents`。**

另外，普通 `pip install openai-agents` 安装的是包源提供的发行版本，**不会自动使用你 clone 下来的这个目录**。本地源码与已安装版本是否相同，是另一件事。

三类内容可以这样区分：

| 类别 | 作用 | 本地例子 |
|---|---|---|
| A. 公共接口 | 让应用开发者调用 | `Agent`、`Runner`、`Session`、`FunctionTool` |
| B. 内部实现 | 实现这些接口背后的运行过程 | `AgentRunner`、`run_internal/` 中的执行函数 |
| C. 项目维护文件 | 帮助维护者测试、打包、发布和协作 | `.github/`、`Makefile`、`AGENTS.md`、开发技能 |

**公共接口与内部实现不一定分属不同目录。** 例如，`run.py` 同时包含公共的 `Runner` 和内部的 `AgentRunner`。

### 为什么 `src/agents/__init__.py` 很重要？

因为 [src/agents/__init__.py](src/agents/__init__.py) 集中展示了顶层 `agents` 包准备提供哪些能力。例如：

```python
from .agent import Agent
from .run import Runner
```

它还导出工具、交接、Session、追踪、结果类型等，并通过 `__all__` 汇总名称。

你第一次阅读它，只需要做两件事：

1. 识别熟悉的公共名称。
2. 顺着导入找到真正定义它的模块。

**它是入口索引，不是主运行程序。** 而且它不是全部公共接口的唯一目录：`agents.decorators`、`agents.mcp` 等子包也有公共能力。本地 `SQLiteSession` 还使用了按需导入，因此不能只看文件开头的普通导入语句。

## 第二部分：整个仓库的地图

以下项目均在分析时的本地版本中存在。星级按你目前“先学普通文本 Agent”的目标评定：⭐⭐⭐ 现在重点看；⭐⭐ 后面再看；⭐ 暂时可以忽略。

| 部分 | 它做什么 | 为什么需要 | SDK 使用者 | 源码学习者 |
|---|---|---|---|---|
| `src/` | 存放可发布源码 | 把库代码与示例、测试分开 | ⭐ 知道位置即可 | ⭐⭐⭐ 找实现的入口 |
| `src/agents/` | SDK 实际实现 | 提供 Agent 运行能力 | ⭐⭐ 按需查 | ⭐⭐⭐ 核心 |
| `examples/` | 展示 SDK 的组合用法 | 把接口变成可理解的程序 | ⭐⭐⭐ | ⭐⭐⭐ 先从这里出发 |
| `tests/` | 检查组件行为与回归 | 避免修改破坏已有行为 | ⭐ | ⭐⭐ 卡在行为细节时查 |
| `integration_tests/` | 验证打包后安装、跨组件及外部服务场景 | 防止源码测试通过但发行包不可用 | ⭐ | ⭐ 后期再看 |
| `docs/` | 使用文档和 API 参考的源码 | 教开发者正确使用 SDK | ⭐⭐⭐ | ⭐⭐⭐ 与示例配合 |
| `.github/` | 自动检查、发布流程、维护脚本和模板 | 让多人协作有一致流程 | ⭐ | ⭐ |
| `.agents/` | 仓库维护技能与架构参考 | 指导代码修改、审查和验证 | ⭐ | ⭐⭐ 按问题选读参考 |
| `README.md` | 项目介绍与最小入口 | 帮读者迅速上手 | ⭐⭐⭐ | ⭐⭐⭐ 看完概览即可 |
| `pyproject.toml` | 包名、依赖、可选能力、构建和工具配置 | 定义 Python 项目如何安装与检查 | ⭐⭐ | ⭐⭐ |
| `uv.lock` | 锁定具体依赖版本 | 让开发环境可复现 | ⭐ | ⭐ |
| `Makefile` | 汇总常用维护命令 | 统一测试、格式检查、构建入口 | ⭐ | ⭐ |
| `AGENTS.md` | 仓库贡献与操作规则 | 约束修改和维护方式 | ⭐ | ⭐ 需要改仓库时再重点看 |

这里有两个容易混淆的地方：

- **`.agents/` 不是应用中的 Agent 实现；`src/agents/` 才是。**
- 本地 `integration_tests/` 特别强调测试**构建后的安装包**，不能只把它理解成“更大的单元测试”。依据：[集成测试说明](integration_tests/README.md)。

根目录还有 `mkdocs.yml`、`PLANS.md`、编辑器配置、许可证和安全说明。现在知道它们分别服务于文档站点、维护规划、编辑环境和项目治理就够了。

## 第三部分：按角色拆解 src/agents

不要把这里看成几十个 Python 文件。把它看成围绕 Runner 工作的一组部件。

| 功能区 | 解决什么问题 | 核心源码 | 与其他模块的关系 | 现在深入程度 |
|---|---|---|---|---|
| Agent 定义 | 描述角色、指令和可用能力 | `agent.py` | Runner 读取它的配置 | ⭐⭐⭐ 看主要字段 |
| 运行协调 | 决定调用模型、执行工具、换人或结束 | `run.py`、`run_internal/` | 串起整个系统 | ⭐⭐⭐ 只追主干 |
| 模型调用 | 把统一请求交给具体模型服务 | `models/interface.py`、`models/openai_responses.py` | 接收 Runner 的输入，返回统一结果 | ⭐⭐⭐ 看接口和一次请求 |
| 工具 | 让模型能请求执行 Python 业务逻辑 | `decorators.py`、`tool.py`、`function_schema.py` | 模型选工具，运行层执行工具 | ⭐⭐⭐ |
| 多 Agent 交接 | 把后续处理移交给另一个 Agent | `handoffs/`、`agent.py` 的 `as_tool()` | 运行层切换 Agent，或启动嵌套运行 | ⭐⭐⭐ |
| 对话历史 | 跨多次调用保留对话记录 | `memory/session.py`、`memory/sqlite_session.py` | Runner 在运行前后读取、写入 | ⭐⭐⭐ 看接口，略过数据库细节 |
| 检查规则 | 检查输入、输出或工具参数是否满足要求 | `guardrail.py`、`tool_guardrails.py` | 由运行层在对应阶段调用 | ⭐⭐ 先懂用途 |
| 执行记录 | 回答“运行过程中发生了什么” | `tracing/` | 记录模型、工具、交接等步骤 | ⭐⭐ 学会查看，暂不读底层 |
| MCP 接入 | 把外部服务提供的工具接进来 | `mcp/server.py`、`mcp/util.py` | 将外部工具接入 Agent 的工具集合 | ⭐ 后面学 |
| 实时交互 | 支持持续连接下的实时会话 | `realtime/` | 有自己的 `RealtimeRunner` 和 Session | ⭐ 暂缓 |
| 语音流水线 | 把音频转文字，再把回答转音频 | `voice/pipeline.py` | 中间可连接 Agent 工作流 | ⭐ 暂缓 |
| 工作环境 | 让 Agent 操作文件、命令和工作区 | `sandbox/sandbox_agent.py`、`sandbox/` | 在普通 Agent 基础上增加环境配置和生命周期 | ⭐ 暂缓 |
| 数据与配置 | 统一表示请求、结果、运行参数和上下文 | `items.py`、`result.py`、`run_context.py`、`run_config.py`、`model_settings.py` | 各层之间传递这些对象 | ⭐⭐ 按调用链认识 |
| 扩展与支撑 | 提供额外适配、测试工具及通用帮助函数 | `extensions/`、`testing/`、`util/`、若干 `_` 开头模块 | 支持主功能 | ⭐ 按需查 |

这里的“运行协调”，也常叫 **orchestration，编排**：解决的就是“这些部件接下来按什么顺序工作”。

几个基础文件只需先建立对应关系：

- `items.py`：一次运行过程中的消息、工具调用、工具结果等记录。
- `result.py`：把整次运行的结果交还应用。
- `run_config.py`：这次运行的配置。
- `model_settings.py`：发送给模型的参数设置。
- `run_context.py`：让工具、检查函数等访问应用提供的 Python 数据。
- `lifecycle.py`：运行到某些阶段时调用你的回调，例如工具开始或结束。
- `run_state.py`：暂停后继续执行所需要的运行状态，第一轮阅读可以略过。

**`context` 不是自动发送给模型的聊天上下文。** 本地代码明确说明，它主要用于把应用数据和依赖传给 Python 代码；要让模型知道其中的信息，需要通过指令、输入或工具输出提供。依据：[RunContextWrapper](src/agents/run_context.py)。

## 第四部分：SDK 的主干，以及谁控制循环

先看关系：

```text
用户代码
   │
   ├── Agent：指令、模型设置、Tools、Handoffs、检查规则
   ├── input：本次用户输入
   └── Session：可选的历史记录
   │
   ▼
Runner
   │
   ├── 准备本次运行，读取历史，安排输入检查
   │
   ▼
调用当前 Agent 对应的 Model
   │
   ▼
模型返回：消息、工具调用、交接请求等
   │
   ▼
SDK 解析并处理这些输出
   │
   ├── 得到最终输出 ──→ 输出检查 ──→ RunResult
   │
   ├── 普通工具调用 ──→ 执行 Python 工具
   │                         │
   │                         └── 结果加入输入 ──→ 下一次模型调用
   │
   ├── Handoff ──→ 当前 Agent 换成目标 Agent ──→ 继续循环
   │
   └── 需要审批 ──→ 返回中断状态，等待以后恢复

Session：在相关阶段保存本次新增记录
Tracing：贯穿执行过程，记录发生了什么
```

这是学习用的主干图。实际一次模型响应可能包含多个输出项，SDK 会统一处理，不是只能四选一。

### 谁控制整个循环？

准确答案分三层：

1. **`Runner` 是你调用的公共入口。**
2. **`run.py` 中的 `AgentRunner._run_impl()` 控制普通非流式运行的外层循环。**
3. **`run_internal` 执行一轮并计算下一步，外层据此继续或返回。**

本地 [run.py](src/agents/run.py) 的 `_run_impl()` 中能看到实际的 `while True`。

> **模型决定“建议做什么”，SDK 的运行代码决定“如何执行、是否继续、如何结束”。**

模型没有直接接管 Python 程序的控制流。

另一个重要区分：

```text
用户发一条消息
    → 一次 Runner.run()
        → 可能多次调用模型
        → 中间可能执行多个工具
    → 一个运行结果
```

“用户的一轮对话”不等于“只调用一次模型”。

## 第五部分：沿着 hello_world 追一次源码

先纠正示例差异。本地 [hello_world.py](examples/basic/hello_world.py) 实际使用的是：

```python
result = await Runner.run(agent, "Tell me about recursion in programming.")
```

它要求 Agent 用俳句回答，最外面用 `asyncio.run(main())` 启动。

原问题给出的 `Runner.run_sync(...)` 则是同步入口。两条路径会汇合到同一个普通异步运行主干。

### 源码调用主线图

```text
examples/basic/hello_world.py
│
├── Agent(...)
│     └── agent.py：创建 Agent 配置对象
│
└── await Runner.run(...)
      └── run.py：Runner.run()
            └── 默认 AgentRunner.run()
                  └── AgentRunner._run_impl()
                        │
                        ├── 初始化配置、上下文、历史、追踪
                        │
                        └── 外层循环
                              │
                              ▼
                    run_internal/run_loop.py
                    run_single_turn()
                              │
                              ├── 读取 instructions
                              ├── 准备输入、工具、交接、输出类型
                              │
                              ▼
                    get_new_response()
                              │
                              ▼
                    Model.get_response()
                              │
                     默认 Responses HTTP 路径
                              ▼
                    models/openai_responses.py
                    OpenAIResponsesModel
                              │
                              ▼
                    AsyncOpenAI.responses.create(...)
                              │
                         返回 ModelResponse
                              ▼
                    run_internal/turn_resolution.py
                    get_single_step_result_from_response()
                              │
                              ├── process_model_response()
                              └── execute_tools_and_side_effects()
                                      │
                                      ▼
                              SingleStepResult.next_step
                                      │
                                      ▼
                    run.py：继续 / 换 Agent / 中断 / 返回
                                      │
                                      ▼
                    RunResult(final_output=...)
```

### ① Agent、Runner 分别来自哪里？

顶层 `agents/__init__.py` 重新导出：

- `Agent`：定义在 [agent.py](src/agents/agent.py)。
- `Runner`：定义在 [run.py](src/agents/run.py)。

### ② Agent(...) 主要保存什么？

主要是：

```text
名字 + 指令 + 模型设置
     + 工具列表
     + 可交接的 Agent
     + 输入/输出检查
     + 输出类型
```

创建对象会进行配置处理和验证，但**不会因为创建了 Agent 就开始一轮模型对话**。

### ③ Runner.run_sync(...) 进入哪里？

进入 `run.py`，再委托给默认 `AgentRunner`：

```text
Runner.run_sync()
    → AgentRunner.run_sync()
    → AgentRunner._run_sync_impl()
```

其中 `AgentRunner` 在源码中明确标注为内部实验实现，不应把它当应用开发接口来直接使用或继承。

### ④ 同步入口为什么能运行异步流程？

因为它会驱动一个 **事件循环**——可以理解为负责推进异步任务的调度器。

本地实现会获取或创建线程默认事件循环，然后：

```text
为 self.run(...) 创建异步任务
    → run_until_complete(task)
    → 等待任务完成
    → 返回 RunResult
```

这里不能简化成“源码就是 `asyncio.run()`”。本地实现特意复用默认循环。依据：[同步桥接实现所在文件](src/agents/run.py)，定位 `AgentRunner._run_sync_impl()`。

在已经运行事件循环的环境中，例如异步函数或常见 Notebook 场景，应使用 `await Runner.run(...)`。

### ⑤ Runner 如何开始一次 run？

`_run_impl()` 会准备：

- 本次 `RunConfig`。
- Python 运行上下文。
- 用户输入与可选 Session 历史。
- 当前 Agent。
- 执行记录、轮数、检查与追踪。

然后进入循环。

初学阶段，只沿“新运行、没有恢复状态、没有 sandbox”的分支阅读。

### ⑥ input 和 instructions 怎样变成模型请求？

在 `run_single_turn()` 中：

- `Agent.get_system_prompt()` 取得 `instructions`。
- 用户输入被整理成输入项。
- 后续轮次还会加入前面生成的消息、工具调用及工具结果。
- 有 Session 时，运行入口已准备相应历史。

在默认 Responses 适配器中，它们主要进入不同请求字段：

```text
Agent.instructions  → instructions
用户消息和运行历史  → input
工具定义            → tools
```

不是把所有东西随便拼成一个大字符串。

### ⑦ 谁真正调用模型？

`get_new_response()` 先解析要使用的 `Model`，再调用其 `get_response()`。

默认配置下会走 OpenAI Responses 适配器，由它调用底层 `openai` Python 客户端：

```python
await client.responses.create(**create_kwargs)
```

依据：[模型调用边界](src/agents/models/openai_responses.py)。

因此有两层库：

```text
Agents SDK：管理 Agent 工作流
    ↓
openai Python 客户端：发送 API 请求
```

配置其他模型实现时，具体适配器会不同。

### ⑧ 谁判断最终输出、工具、交接和下一轮？

重点看 [turn_resolution.py](src/agents/run_internal/turn_resolution.py)：

- `process_model_response()`：辨认输出项。
- `execute_tools_and_side_effects()`：执行相应动作并决定下一步。

下一步用明确类型表达：

```text
NextStepFinalOutput
NextStepHandoff
NextStepRunAgain
NextStepInterruption
```

它们定义在 `run_internal/run_steps.py`。

### ⑨ 工具结果为什么通常还要发给模型？

模型第一次只说：“我需要查询天气。”

Python 工具取得的是数据，例如“东京，14～20℃，晴”。

模型需要看到这些数据，才能决定：

- 如何回答用户。
- 是否还需要其他工具。
- 是否完成任务。

所以普通函数工具默认执行后再次调用模型。

但不是绝对规则：本地 `Agent.tool_use_behavior` 也允许把工具结果直接作为最终结果，例如 `stop_on_first_tool`。

### ⑩ 怎么变成 RunResult.final_output？

运行层产生 `NextStepFinalOutput(output=...)`，完成适用的输出检查等处理后，`run.py` 构建：

```python
RunResult(
    ...,
    final_output=turn_result.next_step.output,
)
```

以上是省略其他字段的调用示意。依据：[结果构建所在文件](src/agents/run.py)。

普通文本 Agent 通常得到字符串；设置结构化输出类型时，可以得到对应的 Python 对象。

## 第六部分：为什么拆成 run.py 和 run_internal？

原问题的理解大体正确，但需要补充：

> **`run.py` 同时承担公共入口和部分核心协调工作；`run_internal/` 分担单轮执行及各类专门机制。**

可以类比餐厅：

- `run.py`：领班掌握整张订单进度。
- `run_internal/`：备菜、烹饪、出餐、记账等工位。
- 每个工位处理具体事务，再报告“完成了”“还要继续”“需要转交”。

这种拆分让工具执行和历史保存等细节，不必全部挤进外层循环，也方便流式与非流式路径复用部分机制。

第一次阅读只看这些：

| 阅读位置 | 你要回答的问题 |
|---|---|
| `Runner.run()` / `run_sync()` | 公共调用交给了谁？ |
| `AgentRunner._run_impl()` | 循环怎样开始、继续和结束？ |
| `run_loop.py` 的 `run_single_turn()` | 一轮如何准备输入并调用模型？ |
| `turn_resolution.py` 的两个核心函数 | 模型输出如何转换成下一步？ |
| `run_steps.py` | “下一步”有哪些明确状态？ |

其他内部文件先按需要打开：

- `tool_execution.py`：学工具时看。
- `session_persistence.py`：学 Session 时看。
- `turn_preparation.py`：想知道模型、工具怎样选择时看。
- `guardrails.py`：学检查规则时看。
- `streaming.py`、`approvals.py`、`model_retry.py`、`oai_conversation.py` 等：先知道存在。

**不要从 `run.py` 第一行一路读到底。** 它包含恢复、审批、异常、并发检查等大量分支，第一遍应该刻意沿最普通路径走。

## 第七部分：普通 Python 函数如何成为 Tool

先纠正一句常见说法：

> 加上装饰器之后，模型获得的是工具说明和参数格式；真正执行 Python 函数的仍然是 SDK。

本地 [decorators.py](src/agents/decorators.py) 明确写着：

```python
tool = function_tool
```

所以本地示例中的 `@tool`，与这里讨论的 `function_tool` 是同一套机制。

整条链是：

```text
Python 函数
   │ 参数名、类型提示、说明文字
   ▼
@tool / @function_tool
   │
   ├── 生成参数格式说明
   └── 保存实际执行函数的包装器
   ▼
FunctionTool
   │
   ▼
模型适配器把工具说明发送给模型
   │
   ▼
模型返回：工具名称 + JSON 参数 + 调用标识
   │
   ▼
SDK 匹配工具
   │
   ▼
解析、验证参数 → 调用原 Python 函数
   │
   ▼
工具结果与调用标识一起进入运行记录
   │
   ▼
下一次模型请求
```

其中 **JSON Schema** 就是“参数格式说明书”：规定有哪些字段、字段是什么类型、哪些必须填写。它不包含 Python 函数的业务实现。

源码对应关系：

| 环节 | 源码 |
|---|---|
| 装饰器入口与别名 | `decorators.py` |
| 创建工具对象、生成执行包装器 | `tool.py` 的 `function_tool()` |
| 读取函数签名和类型，生成参数格式 | `function_schema.py` |
| 规范严格参数格式 | `strict_schema.py` |
| 转成 Responses 工具定义 | `models/openai_responses.py` 的 `Converter` |
| 识别模型返回的工具请求 | `run_internal/turn_resolution.py` |
| 调度函数工具执行 | `run_internal/tool_execution.py` |
| 参数转回 Python 调用并执行 | `tool.py` 中的调用包装器 |
| 把结果带入下一轮 | `run_internal/run_loop.py` |

本地 [basic/tools.py](examples/basic/tools.py) 最适合入门：它的天气函数返回固定的 `Weather` 数据，**没有实际查询天气服务**，因此你可以专注工具调用机制。

`examples/tools/` 则主要展示搜索、代码执行、Shell、Computer 等具体工具能力。不要把那个目录当成学习普通函数工具的第一站。

## 第八部分：Handoff 与 Agent as Tool

关键就是：**谁继续负责后续任务？**

| 问题 | A handoff 给 B | A 把 B 当工具 |
|---|---|---|
| 含义 | 把接待工作交给 B | 请 B 完成一个子任务 |
| 外层当前 Agent | 从 A 变成 B | 仍然是 A |
| B 完成后 | 不会自动回到 A | 结果返回 A 的工具调用流程 |
| 后续通常由谁回答 | B，或 B 后来交接的 Agent | A 继续组织答案 |
| 输入特点 | 使用交接历史，可配置过滤或映射 | 接收这次工具调用构造的输入 |
| 适用场景 | 分诊、转客服、转语言专家 | 翻译、审核、计算、局部研究 |

```text
Handoff

用户 → A → 交接 → B → 用户
                 当前负责人变成 B
```

```text
Agent as Tool

用户 → A ──请求子任务──→ B
       A ←──子任务结果── B
       │
       └──继续判断与回答 → 用户
```

真实实现：

- Handoff：`handoffs/` 定义交接对象；`turn_resolution.py` 执行交接；`run.py` 更新 `current_agent`。
- Agent as Tool：[Agent.as_tool() 所在文件](src/agents/agent.py) 把 Agent 包装成工具，普通路径内部再次调用 `Runner.run()`，提取结果返回外层。

建议对照两个本地例子：

- [routing.py](examples/agent_patterns/routing.py)：按语言交给不同 Agent。
- [agents_as_tools.py](examples/agent_patterns/agents_as_tools.py)：主 Agent 调用不同翻译 Agent。

第二个示例最后还由**应用代码**额外运行了一个汇总 Agent。那一步是示例自己安排的，不是 `as_tool()` 自动产生的行为。

## 第九部分：Session / Memory 到底是什么

本地确实把 Session 实现放在 `src/agents/memory/`。

从当前职责可以这样理解：

- **memory**：这个模块负责保留历史信息。
- **Session**：以一次会话为单位，访问这些历史记录的具体接口。

这是功能分类与接口命名的关系。当前代码可以证明职责，不能据此推断维护者最初命名时的历史动机。

没有 Session，也没有手动传历史时：

```text
run("我叫小明")      → 这次输入只有“我叫小明”
run("我叫什么？")    → 这次输入只有“我叫什么？”
```

即使复用同一个 `Agent` 对象，也不代表自动接上了上一轮对话。

使用同一 Session：

```text
Session 中的历史
       +
本次“我叫什么？”
       ↓
Runner 准备模型输入
       ↓
模型看到之前的“我叫小明”
       ↓
产生回答
       ↓
Runner 保存本次新增记录
```

它解决的工程问题是：

> 不用每次自己读取历史、拼接消息、追加回答，再写回存储。

本地 [Session 接口](src/agents/memory/session.py) 主要要求：

```text
get_items()      取历史
add_items()      加记录
pop_item()       移除最近一项
clear_session() 清空会话
```

Runner 如何连接它，主要看：

- `prepare_input_with_session()`：准备历史与新输入。
- `save_result_to_session()`：保存新增运行记录。

两者都在 `run_internal/session_persistence.py`。

先分清这四个东西：

| 对象 | 保存什么 |
|---|---|
| `Agent` | 角色与能力配置 |
| `Session` | 跨多次运行的对话历史 |
| `context` | 供 Python 工具等使用的应用数据 |
| `RunState` | 暂停后恢复执行所需的状态 |

**Session 不意味着训练模型，也不自动等于长期知识库或检索系统。** 历史能否跨进程保存，取决于所用存储及配置。

没有 Session 也可以多轮对话：应用自行使用 `result.to_input_list()` 传回历史即可。Session 的价值是把这项工作自动化。

## 第十部分：哪些现在可以不学

你的当前目标是普通文本 Agent、Tools、多 Agent。按这个目标排序：

| 能力 | 当前建议 | 理由 |
|---|---|---|
| Agent、Runner、模型输入输出 | 现在学 | 构成最小运行闭环 |
| 普通函数工具 | 现在学 | 让 Agent 能实际做事 |
| Handoff、Agent as Tool | 现在学 | 理解多 Agent 的两种基本组织方式 |
| Session | 接着学 | 支撑普通多轮应用 |
| Guardrail | 理解基本用法 | 让输入输出符合业务要求 |
| Tracing | 学会看执行过程 | 排查“为什么调用了这个工具”很有用 |
| MCP | 第二阶段 | 外部工具的标准化接入方式，不是 Agent 循环的前提 |
| Streaming | 第二阶段 | 改善边生成边展示的体验 |
| Retry | 第二阶段 | 处理暂时性失败，先不读恢复机制 |
| 多模型 Provider | 第二阶段 | 先懂统一接口，再学替换供应商 |
| Realtime、Voice | 暂时忽略 | 属于另一类交互方式 |
| Sandbox | 暂时忽略 | 引入工作环境、资源和恢复管理 |
| Computer、Shell 工具 | 暂时忽略 | 专门执行能力，不是普通工具的必要起点 |
| 各种集成与存储后端 | 按项目需要学 | 第一阶段选一个简单实现即可 |

Tracing 和 Guardrail 属于工程支撑，但值得早建立概念；这不意味着你现在要阅读它们的导出器、并发和异常处理源码。

## 第十一部分：按本地 examples 安排学习路线

以下按学习顺序排列，不按目录字母顺序。

| 阶段 | 看哪个本地 example | 学什么 | 对应源码 | 什么时候可以停 |
|---|---|---|---|---|
| 1 | [basic/hello_world.py](examples/basic/hello_world.py) | Agent、Runner、最终输出 | `agent.py`、`run.py`、`result.py` | 能说明配置何时变成运行 |
| 2 | [basic/tools.py](examples/basic/tools.py) | 函数工具调用闭环 | `decorators.py`、`tool.py`、`function_schema.py` | 能解释模型为何不能直接执行 Python |
| 3 | [agent_patterns/routing.py](examples/agent_patterns/routing.py) | Handoff 与负责人切换 | `handoffs/`、`turn_resolution.py` | 能指出交接后谁继续；先跳过流式事件细节 |
| 4 | [agent_patterns/agents_as_tools.py](examples/agent_patterns/agents_as_tools.py) | 子 Agent 返回结果给主 Agent | `Agent.as_tool()`、工具运行路径 | 能区分交接与调用子任务 |
| 5 | [memory/sqlite_session_example.py](examples/memory/sqlite_session_example.py) | 跨 run 的历史管理 | `memory/session.py`、`session_persistence.py` | 能解释第二个问题为何知道前文 |
| 6 | [agent_patterns/input_guardrails.py](examples/agent_patterns/input_guardrails.py)、[output_guardrails.py](examples/agent_patterns/output_guardrails.py) | 运行前后如何检查业务条件 | `guardrail.py` | 能说明检查对象与失败后果 |
| 7 | [customer_service/main.py](examples/customer_service/main.py) | 把工具、交接、业务 context 组合起来 | 前面各模块、`run_context.py` | 能画出业务流程并定位一个工具 |
| 8 | [research_bot/manager.py](examples/research_bot/manager.py) | 应用代码安排规划、搜索、写作 | `Runner`、结构化结果、追踪 | 能分清应用工作流与 SDK 内部循环 |

阶段 8 很重要：这个例子说明，**多 Agent 并不要求所有步骤都由模型决定**。应用也可以明确编排：

```text
规划 Agent → 执行搜索 → 写作 Agent
```

你先完成阶段 1～5，就已经具备构建基础项目所需的主要概念。

本地其余示例按下面处理：

- `handoffs/`：当前主要是历史过滤示例，掌握基本交接后再看。
- `tools/`：需要搜索等具体能力时选读。
- `mcp/`、`hosted_mcp/`：第二阶段学习外部工具接入。
- `financial_research_agent/`：读懂 `research_bot/` 后再看。
- `model_providers/`、`reasoning_content/`：需要对应模型能力时再看。
- `realtime/`、`voice/`、`sandbox/`：暂时跳过。

阅读大型项目时，每个阶段都使用同一个小循环：

```text
读 example
   ↓
找公共接口
   ↓
追一条普通成功路径
   ↓
用自己的话解释输入、状态变化与输出
   ↓
有具体疑问时再找测试
```

例如，本地有 `test_agent_runner.py`、`test_function_tool_decorator.py`、`test_agent_as_tool.py` 等测试。它们适合回答具体问题，不适合作为第一本教程。

## 第十二部分：学习版项目地图

以下均为分析时真实存在的结构，只保留学习主线。

```text
openai-agents-python/
│
├── examples/                         → 从可理解的使用方式出发
│   ├── basic/
│   │   ├── hello_world.py            → Agent + Runner
│   │   └── tools.py                  → 普通 Python 工具
│   ├── agent_patterns/
│   │   ├── routing.py                → 交接后换负责人
│   │   └── agents_as_tools.py        → 子 Agent 作为工具
│   ├── memory/
│   │   └── sqlite_session_example.py → 多轮历史
│   ├── handoffs/                     → 交接历史过滤，稍后看
│   ├── customer_service/             → 基础能力组合
│   ├── research_bot/                 → 多阶段应用工作流
│   ├── tools/                        → 具体工具能力，按需看
│   └── mcp/                          → 外部工具接入，第二阶段
│
├── src/
│   └── agents/
│       ├── __init__.py               → 顶层公共接口索引
│       ├── agent.py                  → Agent 配置与 as_tool()
│       ├── run.py                    → 公共运行入口与外层协调
│       ├── run_internal/
│       │   ├── run_loop.py           → 单轮模型调用主线
│       │   ├── turn_resolution.py    → 处理输出，决定下一步
│       │   ├── run_steps.py          → 下一步状态的定义
│       │   ├── tool_execution.py     → 执行工具
│       │   └── session_persistence.py→ 读写会话历史
│       ├── decorators.py             → @tool 等装饰器入口
│       ├── tool.py                   → 工具定义与函数包装
│       ├── function_schema.py        → 函数参数变成格式说明
│       ├── handoffs/                 → Agent 交接
│       ├── models/
│       │   ├── interface.py          → 统一模型调用接口
│       │   └── openai_responses.py   → Responses 适配与请求
│       ├── memory/
│       │   ├── session.py            → 会话历史接口
│       │   └── sqlite_session.py     → 一种存储实现
│       ├── result.py                 → 整次运行的返回结果
│       ├── items.py                  → 消息、工具调用等过程记录
│       ├── run_context.py            → Python 应用数据与依赖
│       ├── run_config.py             → 运行配置
│       ├── model_settings.py         → 模型参数
│       ├── guardrail.py              → 输入输出检查
│       ├── tracing/                  → 执行记录
│       ├── mcp/                      → 第二阶段
│       ├── realtime/                 → 暂缓
│       ├── voice/                    → 暂缓
│       └── sandbox/                  → 暂缓
│
├── docs/                             → 用法说明，与 example 配合
├── tests/                            → 有具体疑问时查行为
├── integration_tests/                → 发行包与集成验证，暂缓
├── pyproject.toml                    → 包与依赖配置
│
├── .github/                          → 项目维护
├── .agents/                          → 维护技能与架构参考
├── AGENTS.md                         → 仓库操作规则
├── Makefile                          → 维护命令
└── uv.lock                           → 依赖版本锁定
```

## 最后：先抓哪 5～8 个文件或目录？

建议锁定下面 **8 个阅读目标**，目录也只读指定主线：

1. **[__init__.py](src/agents/__init__.py)**：知道你调用的名字来自哪里。
2. **[agent.py](src/agents/agent.py)**：理解 Agent 保存什么，以及如何把 Agent 变成工具。
3. **[run.py](src/agents/run.py)**：找到真正控制运行进度的外层循环。
4. **[run_internal/](src/agents/run_internal/)**：只先读 `run_loop.py`、`turn_resolution.py`、`run_steps.py`，理解“一轮变成下一步”。
5. **[tool.py](src/agents/tool.py)**：理解 Python 函数如何连接到模型的工具请求，遇到 Schema 再跳转。
6. **[models/interface.py](src/agents/models/interface.py)**：明确 Runner 与具体模型服务之间的边界。
7. **[handoffs/](src/agents/handoffs/)**：理解多 Agent 如何移交后续处理权。
8. **[memory/session.py](src/agents/memory/session.py)**：理解多轮历史需要怎样的最小存储接口。

你的第一阶段完成标准可以非常具体：**不看源码，也能画出“用户输入 → 模型请求 → 工具执行 → 工具结果回传 → 最终输出”，并解释交接时谁变了、Session 保存了什么。** 达到这一步，再深入异常恢复、流式和高级集成会容易很多。
