# 零基础看懂这个项目：它怎么让 AI 帮你办事？

你不需要先认识 `Runner`、`Session`、`MCP` 这些词，也不需要从第一个文件开始往下读。

这份文档先讲一件事：**你问 AI 一个问题之后，这个项目怎样把问题变成一连串实际动作，最后把答案交给你。** 然后再把文件名放回这个过程里。

第一次只读前六节就够了。后面的文件说明留着遇到名字时查，不用背。

## 一、先别看代码。想象你要做一个天气小助手

你希望自己做一个程序，用户可以对它说：

> “帮我看看东京的天气，再告诉我今天适不适合出去玩。”

这个程序最好能完成下面几件事：

1. 听懂用户在问东京的天气。
2. 找到一个查询天气的方法。
3. 真正去查询。
4. 看懂查到的数据。
5. 用一句自然的话回答用户。

看起来像“问一句，答一句”，中间其实发生了好几步。

**这个仓库提供的，就是帮助你把这些步骤连接起来的代码。**

它的名字是 OpenAI Agents SDK Python。先把这个名字翻译成人话：

- **Python**：这些代码主要用 Python 写，你也可以用 Python 调用。
- **SDK**：别人提前做好的一套开发工具，你拿来搭自己的程序。
- **Agent**：在这里，可以先理解为一个配好了工作要求、还能使用工具的 AI 助手。

你可以用它做天气助手、客服助手、资料查询助手等。具体做哪一种，取决于你给它什么要求、接上什么工具。

### 它已经是一个可以直接使用的聊天软件了吗？

它主要是一套给开发者使用的零件，不是一个完整的聊天网站。

比如你想做一个带登录、聊天页面、用户数据库的产品，这些应用部分通常还需要你来搭。这个 SDK 帮你处理的是其中“AI 怎样运行和使用工具”的部分。

### 它里面藏着一个大模型吗？

没有。

**大模型**是负责理解文字、生成回答的那部分 AI。这个仓库主要负责向模型发送请求，以及处理模型返回的内容。

可以先这样看：

```text
你写的天气助手程序
        ↓ 使用
这个 SDK 提供的运行代码
        ↓ 发送请求
模型服务
```

模型服务可能在远程服务器上。这个仓库里的 Python 代码，不等于模型本身。

## 二、先认识 5 个角色，不要记几十个文件名

还是以天气助手为例。

### 1. Agent：这个助手的“工作说明”

你先告诉程序：

> “我要一个天气助手。它回答要简短，可以使用查天气工具。”

`Agent` 对象主要保存这类配置：它叫什么、应该怎么回答、能用哪些工具。

**仅仅创建 Agent，并不代表已经开始询问模型。** 就像写好了工作说明，还没有派出任务。

### 2. Runner：把一件事从头推进到尾的运行程序

`Runner` 负责推进工作：

> “先问模型。它需要查天气，那就执行工具。工具结果有了，再交给模型。已经有最终答案了，结束。”

你可以先把它叫作“流程负责人”。这只是帮助理解的比喻；它实际上是按代码规则工作的程序。

### 3. Model：负责理解问题和生成内容的模型

模型可能回答：

> “我需要使用查天气工具，城市参数是东京。”

拿到天气数据后，模型再组织自然语言回答。

这里有一个分工：**模型提出工具请求，SDK 执行这个请求。** 模型没有直接跑到你的电脑里执行 Python 函数。

### 4. Tool：真正做某件具体事情的工具

比如一段 Python 代码负责：

- 查询天气。
- 查询订单。
- 计算价格。
- 从你允许的数据源中读取资料。

这些具体能力就是工具，英文叫 `Tool`。

模型擅长理解“我要查什么”；工具负责真正完成查询、计算等动作。

### 5. Result：办完之后交回来的结果

事情做完，SDK 返回一个结果对象。

你通常先取其中的 `final_output`，也就是“这次运行最后交出的答案”。

现在只需要记住：

```text
Agent   → 工作要求是什么？
Runner  → 下一步该做什么？
Model   → 怎么理解问题、怎样回答？
Tool    → 哪段程序真正去办事？
Result  → 最后把什么交给用户？
```

## 三、把这 5 个角色连起来，就懂了项目的主线

假设用户问：“东京天气怎么样？”

下面是一种典型过程，不代表每次模型都会选择完全相同的步骤：

```text
① 用户输入问题
   “东京天气怎么样？”
          │
          ▼
② Runner 读 Agent 的工作要求
   “你是天气助手，可以使用查天气工具。”
          │
          ▼
③ Runner 把问题和工具说明发给 Model
          │
          ▼
④ Model 提出请求
   “调用查天气工具，城市是东京。”
          │
          ▼
⑤ SDK 执行 Tool 对应的 Python 代码
   得到：东京，晴，14～20℃
          │
          ▼
⑥ Runner 把工具结果再交给 Model
          │
          ▼
⑦ Model 组织回答
   “东京晴，14～20℃，出门可以带件薄外套。”
          │
          ▼
⑧ Runner 把最终答案装进 Result，返回给你的程序
          │
          ▼
⑨ 你的程序把答案显示给用户
```

### 为什么查完天气还要再问一次模型？

因为查询工具只负责查数据，不一定负责把数据整理成用户需要的答案。

第一次找模型，是让它判断要做什么；工具查完以后再找模型，是让它根据结果继续处理问题。

所以：**用户问一次，不代表程序只调用一次模型。**

如果问题不需要工具，模型也可以直接回答。工具调用以后再次找模型是普通函数工具的默认流程，SDK 也支持配置成直接返回工具结果，第一遍不用研究这个例外。

### 谁在控制整个过程？

**控制流程的是 Runner 背后的 Python 代码。**

模型返回“需要查天气”，SDK 就去找到相应工具；模型给出可结束的回答，SDK 就收尾并返回结果。

这就是这个项目最重要的部分。后面再多的文件，都可以先问一句：

> “这个文件是在帮助上面哪一步？”

## 四、现在回到项目目录：里面其实装了几类东西

“仓库”就是这个项目放代码、示例、文档和维护文件的地方。

你看到的文件夹很多，是因为里面同时放着“给你使用的代码”和“维护这套代码的东西”。它们不用一起学。

```text
整个项目
│
├── src/agents/       → 真正提供功能的代码
│
├── examples/         → 教你怎样使用这些功能的小程序
│
├── docs/             → 使用说明书
│
├── tests/            → 检查功能有没有写错的程序
│
└── 其他维护文件       → 帮作者管理、检查、打包这个项目
```

### src/agents/：已经做好的零件

`Agent`、`Runner`、工具系统等，主要就在这里。

你写自己的程序时，会使用这里提供的能力。

### examples/：用零件搭出来的演示

比如 `examples/basic/tools.py` 演示了一个能使用天气工具的助手。

它与核心代码的关系是：

```text
examples/basic/tools.py
        ↓ 调用
src/agents/ 提供的 Agent、Runner、工具功能
```

**先看 examples，你能知道“这些东西怎么用”；再看 src，你才是在问“它们为什么能这样用”。**

本地这个天气例子的工具返回的是预先写好的天气数据，没有实际连接天气服务。这正适合入门，因为它让你先看清流程。

### docs/：解释怎么用的说明书

你不确定一个参数是什么意思，可以查这里。第一遍不需要从头读完。

### tests/：作者用来确认代码没坏的检查程序

例如作者可能检查：“工具返回结果以后，Runner 有没有正确继续？”

普通用户运行助手时，不需要把整个测试目录跑一遍。

### 它们彼此怎么相关？

```text
docs/ 解释怎么用 ─────────┐
                        ▼
examples/ 演示怎么用 → src/agents/ 提供实际能力
                        ▲
tests/ 检查行为对不对 ────┘
```

这些目录并不是运行时必须依次经过的关卡。它们是同一个项目的代码、示例、说明和检查材料。

## 五、只把最重要的文件放回刚才那条流程

下面这些位置都在 `src/agents/` 里面。

| 文件或文件夹 | 用人话说，它管什么 | 对应刚才哪件事 |
|---|---|---|
| `agent.py` | 保存助手的工作要求 | 定义“你是天气助手” |
| `run.py` | 接任务，推进整次运行，最后返回 | 从用户问题一直管到结果 |
| `models/` | 按模型服务要求整理请求、发送请求、整理返回值 | 找模型理解问题和回答 |
| `tool.py` | 把可执行的功能描述成工具，并准备调用方式 | 让“查天气”成为可用能力 |
| `run_internal/` | 分担运行中的具体工作 | 准备一轮输入、处理回复、执行工具等 |
| `result.py` | 规定运行结果怎样交给应用 | 返回最后答案和过程记录 |
| `__init__.py` | 把常用能力集中到一个方便导入的入口 | 让你能写 `from agents import Agent, Runner` |

关系可以画得更具体一点：

```text
你的代码，或者 examples 里的示例
                │
                ▼
            run.py
                │
                ├── 读取 agent.py 中对象保存的要求
                │
                ├── 让 run_internal/ 处理这一轮
                │        │
                │        ├── 通过 models/ 调用模型
                │        └── 需要时执行 tool.py 定义的工具
                │
                └── 用 result.py 中的结果类型返回答案
```

### 为什么既有 run.py，又有 run_internal/？

因为“把一件事从头推进到尾”会遇到很多细节。

例如，准备输入是一件事，执行工具是一件事，保存记录又是一件事。如果全写在一个文件里，会越来越难看懂。

所以可以先理解成：

- `run.py` 管整个任务的进度。
- `run_internal/` 里的代码分工处理具体步骤。

注意：本地普通运行的外层循环仍然在 `run.py` 中，并不是所有运行代码都搬进了 `run_internal/`。

以后真要读这个文件夹，只先找三个文件：

| 文件 | 只问它一个问题 |
|---|---|
| `run_loop.py` | 这一轮怎样准备输入、调用模型？ |
| `turn_resolution.py` | 模型回复以后，是执行工具、交接，还是结束？ |
| `tool_execution.py` | 决定调用工具后，怎样把它执行起来？ |

其他文件现在可以先折叠起来。

## 六、你自己使用它时，通常只写很少的代码

下面是一个最小用法。先看中文注释，不需要立刻运行，也不要求你看懂每个符号：

```python
from agents import Agent, Runner

# 写好这个助手的工作要求。
assistant = Agent(
    name="小助手",
    instructions="用简单的中文回答问题。",
)

# 交给它一个问题，让 SDK 推进整个过程。
result = Runner.run_sync(assistant, "为什么天空是蓝色的？")

# 把最后答案显示出来。
print(result.final_output)
```

真正要理解的只有三件事：

```text
创建 Agent：告诉助手怎么工作
        ↓
调用 Runner：让它开始处理问题
        ↓
读取 final_output：拿到最后答案
```

`run_sync` 可以先理解成“等整个任务办完，再返回结果”。本地 `hello_world.py` 用的是另一种写法：`await Runner.run(...)`。它适合异步程序，两者背后使用同一套普通运行主线。

**异步**是在等待网络等操作时，允许程序安排其他工作的写法。第一遍看到 `async`、`await`、`asyncio`，先知道它们与“等待和调度”有关即可，不必在这里停下来学习整套异步编程。

实际运行示例还需要安装依赖，并配置相应模型服务的访问方式；上面的代码用于认识结构，不是完整环境安装说明。

### 安装包和本地文件夹有什么关系？

当你执行 `pip install openai-agents`，通常是在 Python 环境中安装别人打包好的版本。这个包主要来自仓库里的 `src/agents/`。

```text
作者的 src/agents/ 代码
         ↓ 打包发布
你安装的 openai-agents 包
         ↓ 在 Python 中导入
from agents import Agent, Runner
```

名字有点绕，但记住：**安装时叫 `openai-agents`，代码里导入时叫 `agents`。**

普通安装不会自动使用你 clone 下来的这个目录。你现在浏览的源码版本，和你的 Python 环境安装的版本，也可能不同。

---

**读到这里，你已经完成第一轮入门。** 你应该能够说出：这个项目帮你推进 AI 助手的工作；模型负责理解和生成内容；工具负责执行具体动作；Runner 把它们连起来。

下面是按需查阅区，不要求一次读完。

## 七、几个经常把初学者绕晕的问题

### 普通 Python 函数，怎么变成 AI 能用的工具？

假设你已经有一个叫 `get_weather` 的函数，负责查询天气。

你需要让模型知道：

> “这里有个工具叫 get_weather，它可以查询天气，你需要给它一个城市名。”

本地示例用 `@tool` 帮你生成这样的说明，并保留实际调用函数的办法。

```text
你的 Python 函数
       ↓ 加上 @tool
SDK 生成“工具说明”和“参数要求”
       ↓
模型看到说明，提出调用请求
       ↓
SDK 把请求中的城市名传给 Python 函数
       ↓
Python 函数真正执行
```

比如模型提出的请求可以理解为：

```text
工具：get_weather
参数：城市 = 东京
```

这不意味着模型看懂并执行了整个 Python 文件。**模型看的是能力说明，实际函数由 SDK 调用。**

如果你遇到这些名字：

- `decorators.py`：提供 `@tool` 这种方便的写法。
- `function_schema.py`：把“需要一个城市名”等参数要求整理成明确格式。
- `JSON Schema`：就是一份给程序看的参数格式说明书。
- `function_tool`：本地 `tool` 是它的别名，先当成同一套工具机制即可。

### 为什么助手有时不记得我刚才说过什么？

因为新的一次运行，不一定自动带着旧对话。

如果你两次只发送下面这些内容：

```text
第一次：“我叫小明。”
第二次：“我叫什么？”
```

第二次的模型不一定能看到第一次的消息。复用同一个 Agent 对象，也不代表自动记住所有聊天。

`Session` 就是帮你管理这一份会话记录的东西：

```text
旧对话 + 新问题
       ↓
交给下一次运行
       ↓
把新产生的记录保存回去
```

相关代码放在 `memory/`。这里先把 memory 理解成“保留对话资料的地方”，Session 理解成“按会话存取记录的接口”。

这不是把新知识训练进模型。保存在哪里、关掉程序后还在不在，要看你选的存储方式和配置。

### Session 和 context 是不是一回事？

不是。

| 名字 | 用天气或客服助手举例 |
|---|---|
| Session | 聊天记录：“刚才用户问的是东京。” |
| context | 程序内部要用的数据，例如用户编号、数据库连接 |

`context` 里的 Python 数据**不会自动全部发给模型**。工具等代码可以用它查资料，再把必要结果交给模型。

### 多个 Agent，是不是多个 AI 同时聊天？

不一定。它们可以是不同的工作要求，甚至使用同一个模型服务。关键是你怎样安排分工。

这个 SDK 有两种常见安排。

**第一种：转交负责人，叫 Handoff。**

```text
用户找总客服
      ↓
总客服发现是退款问题
      ↓
交给退款助手
      ↓
退款助手接着处理
```

交接后不会自动回到总客服。后续由新的助手继续，除非又安排了其他交接。

代码主要在 `handoffs/`。

**第二种：请另一个 Agent 帮忙做一小件事，叫 Agent as Tool。**

```text
总客服让翻译助手翻译一句话
      ↓
翻译助手把结果交回总客服
      ↓
总客服继续回答用户
```

总客服仍然负责外层任务。相关入口是 `agent.py` 中的 `as_tool()`。

记住这两个词就够了：**Handoff 是转交；Agent as Tool 是请人帮忙后拿回结果。**

## 八、剩下“乱七八糟的代码”，大多在处理这些实际问题

最简单的流程很短。真实使用时，还会遇到下面这些问题，所以代码才会变多。

### 第一类：让过程有记录、有结果

| 代码名字 | 它解决的实际问题 |
|---|---|
| `items.py` | 把用户消息、模型回答、工具请求和工具结果表示成程序能区分的记录 |
| `result.py` | 把整次运行的答案和有关记录交给你的程序 |
| `usage.py` | 记录模型调用的用量，帮助了解运行消耗 |
| `tracing/` | 记录一次任务经过哪些步骤，帮助查“为什么这样回答” |
| `logger.py` | 让代码输出调试和运行信息 |

`Tracing` 可以理解成“任务过程录像的文字版”：你不只看到最后一句话，还能了解中间发生过什么。

### 第二类：给运行过程加规则

| 代码名字 | 它解决的实际问题 |
|---|---|
| `guardrail.py` | 运行你提供的输入、输出检查，例如是否符合业务要求 |
| `tool_guardrails.py` | 对工具输入或输出做检查 |
| `run_config.py` | 保存整次运行的设置 |
| `model_settings.py` | 保存调用模型时用的参数 |
| `lifecycle.py` | 允许你的代码在某些时刻收到通知，例如工具开始或结束 |
| `run_internal/approvals.py` | 处理需要先批准才能继续的工具操作 |

`Guardrail` 可以先叫“检查规则”。它运行你设置的检查，不是自动保证 AI 永远正确的开关。

### 第三类：处理没那么顺利的情况

| 代码名字 | 它解决的实际问题 |
|---|---|
| `exceptions.py` | 用不同错误类型区分发生了什么问题 |
| `retry.py`、`run_internal/model_retry.py` | 在符合条件时重试失败的模型请求 |
| `run_error_handlers.py` | 让应用对某些运行错误安排处理办法 |
| `run_state.py` | 保存暂停执行后继续所需要的状态 |

例如“网络出错”“工具需要批准”“运行到一半要恢复”，都会增加代码。但这些不是你理解第一个文本助手的前提。

### 第四类：让输入输出的格式更明确

| 代码名字 | 它解决的实际问题 |
|---|---|
| `function_schema.py`、`strict_schema.py` | 明确工具参数应该长什么样 |
| `agent_output.py` | 让输出可以按指定结构返回，例如分开的姓名、城市字段 |
| `prompts.py` | 支持更灵活地提供提示内容与配置 |
| `tool_context.py` | 给工具执行提供相关数据和调用信息 |

你现在可以只用普通文字输入、普通文字回答。暂时不用研究复杂格式。

### 第五类：增加新的能力

| 代码名字 | 用人话解释 | 什么时候学 |
|---|---|---|
| `mcp/` | 按一种约定好的方式连接外部工具服务 | 需要接入已有工具服务时 |
| `stream_events.py`、内部流式处理代码 | 让应用在结果没完全生成时就逐步收到内容 | 要做边生成边显示的聊天界面时 |
| `realtime/` | 处理持续连接的实时交互 | 要做实时会话时 |
| `voice/` | 把语音转成文字，把文字回答转成语音 | 要做语音助手时 |
| `sandbox/` | 管理 Agent 工作的文件环境、命令执行和工作区 | 要让 Agent 操作项目或文件时 |
| `computer.py` | 定义鼠标、键盘等计算机操作能力的接口 | 要做电脑操作助手时 |
| `editor.py`、`apply_diff.py` | 支持文件补丁和编辑相关操作 | 要做代码编辑助手时 |
| `extensions/` | 放额外适配和辅助能力 | 需要其中某个功能时 |

MCP 的全称是 Model Context Protocol，当前先理解成“工具服务之间统一的连接约定”。会使用普通 Python 工具之后，再学它会容易很多。

`Realtime` 和 `Voice` 都与实时或语音体验相关，但不是同一个东西。现在不用研究它们之间的实现差异。

### 第六类：给其他代码打下手

还有 `util/`、`testing/` 和许多以下划线开头的文件，例如 `_tool_identity.py`。

它们常常处理公共小操作、确定工具身份、支持测试等问题。下划线开头通常是在提示“这是内部使用的东西”，不代表它没有用，也不代表你现在要理解。

看到一个陌生内部文件，先跳过。只有当你正在追的流程真的需要它时，再点进去。

## 九、项目根目录里，还有一些不是 AI 功能的文件

这些文件主要帮助项目安装、维护和协作。看不懂它们，不影响你先理解 Agent。

| 名字 | 可以先怎样理解 |
|---|---|
| `README.md` | 项目门口的介绍牌 |
| `pyproject.toml` | 项目的身份、依赖和构建设置 |
| `uv.lock` | 记录开发环境具体用了哪些依赖版本 |
| `Makefile` | 把常用维护命令集中起来 |
| `.github/` | 代码托管平台上的自动检查和协作设置 |
| `integration_tests/` | 检查打包安装、外部服务等组合起来是否能工作 |
| `.agents/` | 给维护这个仓库的编程助手使用的技能和参考材料 |
| `AGENTS.md`、`CLAUDE.md` | 给编程助手看的仓库工作说明 |
| `PLANS.md` | 说明复杂开发任务怎样记录计划 |
| `mkdocs.yml` | 文档网站的配置 |
| `pyrightconfig.json` | 帮助检查 Python 类型使用的配置 |
| `.idea/`、`.vscode/` | 编辑器相关设置 |
| `.git/`、`.gitignore`、`.gitattributes` | 版本记录，以及哪些文件怎样交给版本管理工具处理 |
| `LICENSE`、`SECURITY.md` | 使用许可和安全问题说明 |
| `__pycache__/` | Python 运行时生成的缓存，不需要手工阅读或修改 |

你本地还可能有自己的脚本和 `.env` 等配置文件。`.env` 常用来保存运行配置，也可能包含密钥，不要把内容随意分享出去。本文没有读取其内容。

特别记住这两个名字：

```text
src/agents/ → 你要学习和使用的 Agent 代码
.agents/    → 帮维护者管理开发工作的材料
```

它们看着像，做的事不一样。

## 十、从今天开始，按这 5 步看就够了

不要求你先通读源码，也不用一次安装所有可选功能。

### 第 1 步：看“创建助手 → 开始运行 → 打印答案”

打开 [examples/basic/hello_world.py](examples/basic/hello_world.py)。

只找 `Agent(...)`、`Runner.run(...)`、`result.final_output`。先略过异步启动的细节。

**能说出这三处各负责什么，就停。**

### 第 2 步：看工具怎样加入助手

打开 [examples/basic/tools.py](examples/basic/tools.py)。

只看三个位置：`get_weather` 做什么、`@tool` 放在哪里、`tools=[get_weather]` 怎样把它交给 Agent。

**能解释“模型请求查天气，Python 函数返回数据”，就停。** 不需要先读参数格式生成代码。

### 第 3 步：看两个助手怎样分工

先看 [examples/agent_patterns/agents_as_tools.py](examples/agent_patterns/agents_as_tools.py) 中翻译助手的定义和 `.as_tool(...)`。

再看 [examples/agent_patterns/routing.py](examples/agent_patterns/routing.py) 中的 `handoffs=[...]`。这个例子包含边生成边显示的代码，先不研究那部分。

**能区分“请另一个助手帮忙”与“把后续任务转给它”，就停。**

### 第 4 步：看下一句话怎样带上之前的聊天

打开 [examples/memory/sqlite_session_example.py](examples/memory/sqlite_session_example.py)。

找创建 Session 的地方，再找几次 `Runner.run(..., session=session)`。

**能解释这些运行为什么能使用同一份历史，就停。** 不需要先学数据库实现。

### 第 5 步：现在才进入核心源码

只打开这三个位置：

1. [agent.py](src/agents/agent.py)：找你已经用过的 `name`、`instructions`、`tools`，看看它们保存在什么地方。
2. [run.py](src/agents/run.py)：找 `Runner` 和它交给内部运行对象的调用，先不要从头读完。
3. [run_internal/run_loop.py](src/agents/run_internal/run_loop.py)：找 `run_single_turn`，看看一轮怎样取得指令、准备输入并调用模型。

**你能把看到的代码放回第三节的流程图，就已经在读懂这个项目了。**

## 最后，用这张小地图检查自己有没有理解

```text
你想做一个 AI 助手
        │
        ├── examples/：看看别人怎么用
        ├── docs/：不懂用法时查说明
        │
        ▼
你写自己的应用代码
        │
        ├── Agent：写工作要求
        ├── Tool：提供实际办事的方法
        └── Session：需要多轮聊天时保存历史
        │
        ▼
Runner 推进任务
        │
        ├── 找模型理解和生成内容
        ├── 按请求执行工具
        ├── 必要时换另一个 Agent
        └── 重复这些步骤，直到结束或需要暂停
        │
        ▼
Result 把结果交回你的应用
        │
        ▼
你的应用显示给用户
```

你现在不需要回答“这个仓库每一行代码为什么这样写”。先试着回答这三个问题：

1. 用户问天气以后，是谁真正执行查询？——工具对应的代码。
2. 查完以后，是谁推进到下一步？——Runner 背后的运行代码。
3. 最后是谁根据天气数据组织自然语言回答？——模型。

**能分清这三个角色，你就抓住了这个项目的主干。** 其他文件大多是在让这条主干更可靠，或者让它多做一些事情。

---

本文为项目根目录下的个人学习说明，不属于发布文档或安装教程。依据 2026-09-05 本地源码整理；本轮核对时提交为 `1d471a47`。后续版本的具体文件和实现可能变化。
